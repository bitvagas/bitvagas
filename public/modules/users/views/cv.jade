img(ng-src="{{ user.PICTURE ? user.PICTURE : 'img/unknown.svg' }}" style="max-width:100px")
h1 {{ user.NAME }}
h5 {{ user.HEADLINE }}
h6 {{ user.SUMMARY }}
ul
  li(ng-repeat="skill in user.cv.skills.values")
    label {{ skill.skill.name }}
